project 'AppSmack.xcodeproj'

# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AppSmack' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AppSmack
  pod 'Alamofire'
  pod 'SwiftyJSON'
  pod 'Socket.IO-Client-Swift'

  target 'AppSmackTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AppSmackUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
